import React from 'react';

import { Container, Title } from '../delivery-address/styles';

export default () => {
    return (
        <Container>
            <Title>
                Endereço de entrega
            </Title>
        </Container>
    );
}