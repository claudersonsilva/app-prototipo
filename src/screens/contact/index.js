import React from 'react';

import { Container } from '../../global-styles/styles';

import { Title } from '../contact/styles';

export default () => {
    return (
        <Container>
            <Title>
                Contato
            </Title>
        </Container>
    );
}